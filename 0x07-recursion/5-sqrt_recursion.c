#include "holberton.h"
/**
 * _sqrt_recursion - reset
 * @n: int
 * 
 * Return: 0
**/

int _sqrt_recursion(int n)
{
int n2;
if (n * n)
{
n2 = n - 1;
return (n2 * _sqrt_recursion(n));
}
else if (y == 0)
{
return (1);
}
else
{
return (-1);
}
}
