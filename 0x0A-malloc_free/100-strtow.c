#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * **strtow - Array
 * @str: Pointer string
 * @ : Content of array
 * Return: Null if size == 0, or Address of pointer if size > 0
**/
char **strtow(char *str)
{
char *p = str;

if (*p != '\0')
	return (0);


return (0);
}
