#include "holberton.h"
/**
 * _isdigit - Init function
 * @c: Description of parameter n
 * Description: Longer description of the function)?
 * section header: Section description)*
 * Return: Description of the returned value
 */
int _isdigit(int c)
{
	if (c >= 48 && c < 58)
	{
	return (1);
	}

return (0);
}
