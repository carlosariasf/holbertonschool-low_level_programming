#include "dog.h"
#include <stdlib.h>
/**
 * init_dog - Dog
 * @d: struct
 * @name: char
 * @age: floar
 * @owner: char
**/
void init_dog(struct dog *d, char *name, float age, char *owner)
{
if (d != NULL)
{
if (name != NULL)
	d->name = name;
if (age != age)
{
}
else
{
	d->age = age;
}
if (owner != NULL)
	d->owner = owner;
}
}
